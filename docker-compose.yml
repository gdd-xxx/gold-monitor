services:
  gold-monitor:
    build: .
    container_name: gold-monitor
    environment:
      - CONTAINER_NAME=gold-monitor
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
