services:
  gold-monitor:
    build: .
    container_name: gold-monitor
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
    user: "1000:1000"
